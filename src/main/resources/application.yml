spring:
  application:
    name: event-service
  profiles:
    active:
    - dev
  main:
    allow-bean-definition-overriding: true  
    
server:
  port: 9072
  servlet:
    context-path: /event-service

         